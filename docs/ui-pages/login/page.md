# Login

Sign-in screen (dark FinDash chrome). Reuse as the Stitch ref for auth, session, and device-link pages.

- Module accent: auth / dashboard indigo `#6C63FF`
- Flutter: existing auth/login widgets in `am_app` — do not invent a new login brand
