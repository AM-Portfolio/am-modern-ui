# Legacy single nginx config — superseded by env-specific profiles.
#
# Runtime uses docker-entrypoint.sh + nginx.profiles/:
#   - nocache.conf    → dev, preprod (ENVIRONMENT != prod)
#   - revalidate.conf → prod
#
# See docs/CACHE_STRATEGY.md
