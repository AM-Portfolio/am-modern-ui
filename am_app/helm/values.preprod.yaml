# Preprod Environment — am-modern-ui
environment: preprod

appConfig:
  domain: "am.asrax.in"

replicaCount: 1

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: am.asrax.in
      paths:
        - path: /
          pathType: Prefix
