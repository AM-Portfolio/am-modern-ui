{
  "_comment": "Shared template only — no env host. Override via Helm config.json (cluster) or config.{env}.json / .env (local).",
  "domain": "",
  "services": {},
  "google": {
    "webClientId": ""
  },
  "growthbook": {
    "enabled": true,
    "apiHost": "https://growthbook.asrax.in/gbapi",
    "clientKey": "sdk-GLujAhix8Foe5qoC"
  }
}
