{
  "_comment": "Copy to config.local.json (gitignored, like .env). Merged last over config.{env}.json. Missing file is fine. Example: prod UI + local portfolio.",
  "services": {
    "portfolio": "http://localhost:8072"
  }
}
