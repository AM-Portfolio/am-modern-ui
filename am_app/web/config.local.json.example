{
  "_comment": "Copy to config.local.json (gitignored). Merged last over config.{env}.json. For local UI + preprod APIs + Google on localhost:9000, use domain + webClientId below.",
  "domain": "am-preprod.asrax.in",
  "services": {},
  "google": {
    "webClientId": "307768822337-ad7tee4d82cc0b4flgrfs157e5e6rc0g.apps.googleusercontent.com"
  }
}
