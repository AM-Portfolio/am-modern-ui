/// Enum for grouping allocations
enum GroupBy {
  sector,
  industry,
  marketCap,
  stock,
}

/// Enum for time frame selection
enum TimeFrame {
  oneDay,
  oneWeek,
  oneMonth,
  threeMonths,
  oneYear,
  all,
}
