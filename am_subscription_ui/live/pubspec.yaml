name: am_subscription_ui_example
description: "Standalone runner for Subscription UI"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  am_subscription_ui:
    path: ../
  am_design_system:
    path: ../../am_design_system
  am_common:
    path: ../../am_common
  am_auth_ui:
    path: ../../am_auth_ui

  # State Management
  flutter_bloc: ^9.1.1
  get_it: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
